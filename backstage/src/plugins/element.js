import Vue from 'vue'
import { Button,Menu,MenuItem,MenuItemGroup,Submenu,RadioButton,RadioGroup,Breadcrumb,BreadcrumbItem} from 'element-ui'

Vue.use(Button).use(MenuItemGroup).use(MenuItem).use(Menu).use(Submenu).use(RadioButton).use(RadioGroup)
.use(Breadcrumb).use(BreadcrumbItem)